import { View, Text } from "react-native";
import React from "react";
import FFView from "../FFView";
import FFText from "../FFText";

const Withdraw = () => {
  return (
    <View>
        <FFText>Hello with draw</FFText>
    </View>
  );
};

export default Withdraw;
